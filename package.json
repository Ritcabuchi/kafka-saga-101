{
  "name": "kafka-sage-101",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start:producer": "ts-node-dev --respawn --transpile-only src/producer.ts",
    "start:consumer": "ts-node-dev --respawn --transpile-only src/consumer.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chance": "^1.1.8",
    "kafkajs": "^2.0.1"
  },
  "devDependencies": {
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.7.2"
  }
}
